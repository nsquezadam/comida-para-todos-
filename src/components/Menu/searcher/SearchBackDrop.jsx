import React from 'react'
import './SearchBackDrop.scss';

const SearchBackDrop = (props) => {
    return (
        <div className="backSearchDrop" onClick={props.click} >
            
        </div>
    )
}

export default SearchBackDrop
